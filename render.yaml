services:
  - type: web
    name: star-wars-api
    runtime: docker
    plan: free
    dockerfilePath: ./Dockerfile
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: star-wars-db
          property: connectionString
      - key: JWT_SECRET
        value: super_secret_star_wars_key_12345
      - key: NODE_ENV
        value: production

databases:
  - name: star-wars-db
    plan: free
